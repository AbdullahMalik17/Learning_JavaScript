/* 

<------Prompt-------->
It is used to get input from user .Two values are set in it ,
one is asked from user and another we set as default.
It's syntax as follows:

var var-name= prompt("input");

*/
//For example:
var name=prompt("What is your name?");
document.write(name);