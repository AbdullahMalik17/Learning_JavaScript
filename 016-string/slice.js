/* 
<----------slice()----------->
It is used to take the specific values of string . If we use
negative index in the functiuon(it works oppoit side of counting)

It's syntax as follow:-
variable-name.slice(start of index,end of index);

*/
// For example:-

let a="Hello World"
console.log(a.slice(3,10))   //output lo Worl
console.log(a.slice(3))      //output lo World