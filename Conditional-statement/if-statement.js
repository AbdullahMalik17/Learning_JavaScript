/*

<-------- if statement------->
It executes a part of program when condition 
turns to be true otherwise set of statements are run.

It's syntax as follows:-
if(condition){
set of statement 
}

condition:
          It is an expression which turn result in boolean expression.

*/
// For Example:-
 var b=6;
 if(b>=6){
    console.log(Hello World);
 }